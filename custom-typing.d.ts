declare module '@tensorflow/tfjs-core' {
    export interface Long {
        // Define the properties and methods of the Long type if known
        // For example:
        low: number;
        high: number;
        unsigned: boolean;
    }
}